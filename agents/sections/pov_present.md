## POV Variants

{pov_count} proof-of-vulnerability input(s) were provided:

{pov_list}

You can validate whether an input triggers a crash with `libCRS run-pov` using build ID `base` (the compiled vulnerable version).
Your patch must fix all POV variants — verify each one before submission.
